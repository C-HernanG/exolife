# Example environment configuration for ExoLife
# Copy this file to `.env` and modify values as needed.

# Data directory used by the ExoLife Python package.  The default
# inside the container is `/data`.  You can override this at runtime
# by setting this variable or by mounting your own volume to
# `/data` in docker-compose.
EXOLIFE_DATA_DIR=/data

# Log level for the ExoLife CLI and application.  Valid values are
# DEBUG, INFO, WARNING, ERROR and CRITICAL.
EXOLIFE_LOG_LEVEL=INFO

# Example API key (unused by default).  Use this to inject secrets
# into your containers if needed.
EXOLIFE_API_KEY=your_api_key_here
